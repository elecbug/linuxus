version: "3.8"

services:
  ubuntu-shell:
    build: .
    container_name: ubuntu-shell-student1
    ports:
      - "7681:7681"
    volumes:
      - student1_home:/home/student
    restart: unless-stopped

volumes:
  student1_home: